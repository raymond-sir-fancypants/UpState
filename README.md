# UpState
 A lightweight, dependency-free JavaScript state management library for the browser.  UpState gives you a simple, predictable way to manage application state — with optional persistence to `localStorage` or `sessionStorage`, an event-driven API built on native `EventTarget`, and safe result handling that never throws on missing data.
